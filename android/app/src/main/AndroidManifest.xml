<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ========================================================
         PERMISSIONS
         Every entry below backs a real data source used by the app.
         ======================================================== -->

    <!-- Network: required to reach the FINIX backend API. Previously this was
         declared ONLY in the debug/profile manifests, so release builds could
         not make a single HTTP call and the app silently fell back to its
         built-in mock data. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Lets the app show an "offline" state instead of hanging on a request. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Camera: live QR scanning on the Scan QR screen (mobile_scanner). -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Gallery: picking an existing QR screenshot / KYC document.
         Android 13+ (API 33) uses the scoped media permission; older releases
         fall back to READ_EXTERNAL_STORAGE, which is capped at API 32 so it is
         not requested (and not flagged) on newer devices. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>

    <!-- Biometrics: device-bound step-up auth for high-risk transactions
         (services/hardware_service.dart -> com.finix.hardware/biometric). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Reads the inbox so the security screen can scan real messages for
         fraud. Play restricts READ_SMS to approved use cases; this build is
         sideloaded, and a Play listing would need that declaration. Only the
         inbox is read, and only when the user grants it at runtime. -->
    <uses-permission android:name="android.permission.READ_SMS"/>
    <!-- Dictation in the AI assistant. Recognition runs on the platform
         recogniser, so audio is not uploaded by the app. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Required from Android 13 to show anything on the status bar. Asked for
         at runtime from the notifications screen, not at launch. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>

    <!-- The camera is optional so the app still installs on devices without
         one; the Scan QR screen degrades to gallery-only in that case. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <application
        android:label="Finix"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ package visibility: without this the speech recogniser
             is invisible to the app and SpeechToText.initialize() always
             returns false, so the mic button would never work on a modern
             device. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
